org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

minecraft_version=26.1.2
loader_version=0.19.2
fabric_version=0.150.0+26.1.2

mod_version=1.0.1
maven_group=net.recruitmentaddon
archives_base_name=earthmc-recruitment-addon
modmenu_version=18.0.0-beta.1

# Modrinth launcher profile that `gradlew build` deploys the jar into.
# This is the folder name as it appears under ModrinthApp/profiles, so change it
# here if you rename or duplicate the profile. The launcher suffixes duplicated
# profiles (e.g. "26.1.2 (1)"), so it does not always match minecraft_version.
modrinth_profile=26.1.2 (1)
